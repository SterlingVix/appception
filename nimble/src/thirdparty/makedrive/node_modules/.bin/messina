#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../messina/bin/messina" "$@"
  ret=$?
else 
  node  "$basedir/../messina/bin/messina" "$@"
  ret=$?
fi
exit $ret
